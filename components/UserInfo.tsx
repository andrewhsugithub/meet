import Calendar from "./Calendar";

const UserInfo = () => {
  return (
    <div className="text-center">
      <h1 className="uppercase font-bold text-3xl">My Meetings</h1>
      <Calendar />
    </div>
  );
};

export default UserInfo;
